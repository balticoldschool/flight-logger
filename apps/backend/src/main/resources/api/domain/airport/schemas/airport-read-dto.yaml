---

type: object
properties:
  icao:
    $ref: './common/airport-icao.yaml'
  iata:
    $ref: './common/airport-iata.yaml'
  name:
    $ref: '../../../common/schemas/name.yaml'
  city:
    $ref: '../../../common/schemas/name.yaml'
  country:
    $ref: '../../../domain/country/schemas/country-read-dto.yaml'
  timezone:
    type: string
    description: The IANA timezone identifier of the airport
    example: "Europe/Berlin"
    maxLength: 127
